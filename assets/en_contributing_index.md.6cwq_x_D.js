import{G as e,K as t,at as n,j as r}from"./chunks/framework.DrNIf5Lg.js";var i=JSON.parse(`{"title":"Contributing","description":"","frontmatter":{},"headers":[],"relativePath":"en/contributing/index.md","filePath":"en/contributing/index.md"}`),a={name:`en/contributing/index.md`};function o(r,i,a,o,s,c){return n(),e(`div`,null,[...i[0]||=[t(`<h1 id="contributing" tabindex="-1">Contributing <a class="header-anchor" href="#contributing" aria-label="Permalink to “Contributing”">​</a></h1><p>StaticPHP welcomes fixes, package definitions, platform support, tests, and documentation. v3 is still defining parts of its extension API, so distinguish between a contribution to the built-in <code>core</code> Registry and an external Registry that needs a long-term compatibility contract.</p><h2 id="development-setup" tabindex="-1">Development Setup <a class="header-anchor" href="#development-setup" aria-label="Permalink to “Development Setup”">​</a></h2><p>A source checkout requires PHP 8.4 or later and Composer. The CLI also needs the PHP extensions listed in the <a href="/en/guide/installation.html">source installation guide</a>. Install the development dependencies from the repository root:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bin/spc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --version</span></span></code></pre></div><p>Node.js and npm are only required when changing the VitePress documentation:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:build</span></span></code></pre></div><p>Run <code>bin/spc doctor</code> before attempting a real build. Full static builds are platform-sensitive and can be slow, so they are not a prerequisite for every documentation, test, or configuration-only change.</p><h2 id="find-the-correct-layer" tabindex="-1">Find the Correct Layer <a class="header-anchor" href="#find-the-correct-layer" aria-label="Permalink to “Find the Correct Layer”">​</a></h2><p>Keep a change in the narrowest layer that can express it:</p><table tabindex="0"><thead><tr><th>Area</th><th>Purpose</th></tr></thead><tbody><tr><td><code>config/pkg/ext/</code></td><td>PHP extension Package definitions</td></tr><tr><td><code>config/pkg/lib/</code></td><td>Link-time dependency library definitions</td></tr><tr><td><code>config/pkg/target/</code></td><td>Final and virtual build targets</td></tr><tr><td><code>config/pkg/tool/</code></td><td>Host-side build tool Packages</td></tr><tr><td><code>config/artifact/</code></td><td>Shared or complex source/binary Artifact definitions</td></tr><tr><td><code>src/Package/</code></td><td>Package-specific recipes, patches, hooks, and custom Artifact behavior</td></tr><tr><td><code>src/StaticPHP/</code></td><td>Framework, resolver, runtime, Doctor, Registry, and command implementation</td></tr><tr><td><code>tests/</code></td><td>PHPUnit tests and fixtures</td></tr><tr><td><code>docs/en/</code>, <code>docs/zh/</code></td><td>Canonical English documentation and its synchronized Chinese version</td></tr></tbody></table><p>Prefer declarative YAML fields over PHP conditions. Add a recipe class only when the package needs build commands, source rewrites, validation, custom configure arguments, or lifecycle hooks.</p><p>Do not fix a problem by editing generated <code>buildroot/</code>, <code>source/</code>, <code>downloads/</code>, or <code>pkgroot/</code> content. Changes there are discarded and do not describe how another user can reproduce the build.</p><h2 id="adding-or-updating-a-package" tabindex="-1">Adding or Updating a Package <a class="header-anchor" href="#adding-or-updating-a-package" aria-label="Permalink to “Adding or Updating a Package”">​</a></h2><p>Before writing a new definition, search for a package with the same build system and artifact type. Follow its structure, then verify these points:</p><ol><li>Choose the correct type: <code>php-extension</code>, <code>library</code>, <code>target</code>, <code>virtual-target</code>, or <code>tool</code>.</li><li>Use the exact package name. PHP extension dependencies and configs use the <code>ext-</code> prefix.</li><li>Put hard dependencies in <code>depends</code>, optional relationships in <code>suggests</code>, and host build tools in <code>tools</code>.</li><li>Express platform differences with <code>@windows</code>, <code>@unix</code>, <code>@linux</code>, or <code>@macos</code> fields when the schema supports them.</li><li>Prefer an inline Artifact for a simple one-package source; use <code>config/artifact/</code> when multiple Packages share it or custom behavior makes a standalone definition clearer.</li><li>Record license identifiers and files in Artifact metadata. Add structured Package <code>license</code> entries only for material that must be copied after a source build.</li><li>Add or update the relevant PHP recipe only when configuration alone is insufficient.</li></ol><p>See <a href="/en/develop/package-model.html">Package Model</a> and <a href="/en/develop/artifact-model.html">Artifact Model</a> for the current schemas.</p><p>Configuration linting is strict about unknown fields but cannot prove that a URL exists, a patch still applies, or a library links on every platform. When practical, test the oldest and newest affected PHP branches and the platforms touched by the change.</p><h2 id="php-code-and-tests" tabindex="-1">PHP Code and Tests <a class="header-anchor" href="#php-code-and-tests" aria-label="Permalink to “PHP Code and Tests”">​</a></h2><p>StaticPHP follows the existing code style and favors existing package/build patterns over new abstractions. For framework code:</p><ul><li>Keep internal orchestration details internal; PHP <code>public</code> visibility alone does not make a symbol part of the supported extension API.</li><li>Add a focused PHPUnit test for resolver, config, Registry, command, or utility behavior.</li><li>Include the failing input or regression case in the test instead of relying only on a manual build.</li><li>Avoid unrelated formatting or mechanical changes in the same pull request.</li></ul><p>Tests live under <code>tests/StaticPHP/</code> and use the <code>Tests\\StaticPHP\\</code> namespace. Run the smallest relevant test during development, then the project checks before submitting.</p><h2 id="doctor-changes" tabindex="-1">Doctor Changes <a class="header-anchor" href="#doctor-changes" aria-label="Permalink to “Doctor Changes”">​</a></h2><p>Built-in Doctor checks currently live under <code>src/StaticPHP/Doctor/Item/</code>. A check should observe the environment without changing it; installation, downloads, and file changes belong in a separate fix callback.</p><p>The current Attributes and callback signatures are internal and have known limitations, including raw fix parameters and no automatic re-check after a fix. Follow an existing core check when maintaining the built-in set, but do not present that pattern as a stable third-party API. Update <a href="/en/develop/doctor-module.html">Doctor Module</a> and <a href="/en/develop/system-build-tools.html">Compilation Tools</a> when user-visible checks, fixes, prerequisites, or lock behavior change.</p><h2 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to “Documentation”">​</a></h2><p>English under <code>docs/en/</code> is canonical. Every English Markdown file must have a corresponding file under <code>docs/zh/</code>, and both versions must have the same headings, examples, tables, admonitions, and links to the appropriate language path.</p><p>When behavior changes, update all pages that describe it—not only the closest reference page. Common examples include CLI options, environment variables, Package/Artifact fields, Doctor checks, and migration guidance.</p><p>Validate matching file trees and build the site:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">diff</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">find</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/en </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">-name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;*.md&#39; </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sed</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s|docs/en/||&#39; </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sort</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">     &lt;(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">find</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/zh </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">-name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;*.md&#39; </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sed</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s|docs/zh/||&#39; </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sort</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:build</span></span></code></pre></div><p>New pages must also be added to both VitePress sidebars.</p><h2 id="validation-checklist" tabindex="-1">Validation Checklist <a class="header-anchor" href="#validation-checklist" aria-label="Permalink to “Validation Checklist”">​</a></h2><p>Use checks proportional to the change:</p><table tabindex="0"><thead><tr><th>Change</th><th>Required checks</th></tr></thead><tbody><tr><td>Package, Artifact, target, extension, library, or tool config</td><td><code>bin/spc dev:lint-config</code>, <code>composer cs-fix</code></td></tr><tr><td>PHP framework or recipe code</td><td><code>composer cs-fix</code>, <code>composer test</code>, <code>composer analyse</code></td></tr><tr><td>Documentation</td><td>English/Chinese tree and structure checks, <code>npm run docs:build</code></td></tr><tr><td>Platform build fix</td><td>The checks above plus the narrowest affected build or CI matrix when available</td></tr></tbody></table><p>Useful commands:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bin/spc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev:lint-config</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cs-fix</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> analyse</span></span></code></pre></div><p>If a full build cannot be run locally, state which platform/PHP combinations remain untested and include the relevant logs or CI result. Build and shell logs live under <code>log/</code> by default, but generated logs should not normally be committed.</p><h2 id="pull-requests" tabindex="-1">Pull Requests <a class="header-anchor" href="#pull-requests" aria-label="Permalink to “Pull Requests”">​</a></h2><p>Keep each pull request focused and explain:</p><ul><li>The problem and why the selected layer is appropriate.</li><li>The affected Package/Artifact, target OS, architecture, toolchain, and PHP versions.</li><li>User-visible behavior or compatibility impact.</li><li>Tests and builds that were run, including anything not tested.</li><li>Documentation updated for both languages.</li></ul><p>Complete the repository pull-request checklist. Do not commit generated build directories, downloaded archives, caches, local environment files, or unrelated editor/OS metadata.</p><p>For a new dependency or bundled patch, include its upstream source, license, and the condition that requires it. Prefer an upstream fix and keep downstream patches scoped to the affected versions.</p><h2 id="security-reports" tabindex="-1">Security Reports <a class="header-anchor" href="#security-reports" aria-label="Permalink to “Security Reports”">​</a></h2><p>Do not publish exploit details, credentials, signing material, or a reproducible vulnerability in a normal issue. Use GitHub&#39;s private vulnerability reporting for the repository when available, or contact the maintainers through an existing project channel to arrange a private disclosure.</p><p>The repository does not currently contain a dedicated <code>SECURITY.md</code>; adding a formal supported-version and disclosure policy remains a project maintenance task.</p>`,45)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};