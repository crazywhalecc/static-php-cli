import{G as e,K as t,at as n,j as r}from"./chunks/framework.DrNIf5Lg.js";var i=JSON.parse(`{"title":"Doctor Module","description":"","frontmatter":{},"headers":[],"relativePath":"en/develop/doctor-module.md","filePath":"en/develop/doctor-module.md"}`),a={name:`en/develop/doctor-module.md`};function o(r,i,a,o,s,c){return n(),e(`div`,null,[...i[0]||=[t(`<h1 id="doctor-module" tabindex="-1">Doctor Module <a class="header-anchor" href="#doctor-module" aria-label="Permalink to “Doctor Module”">​</a></h1><p>Doctor diagnoses whether the current host, selected target, and compiler toolchain can run StaticPHP&#39;s build pipeline. It checks common host commands, platform-specific SDKs, managed tool packages, and target-aware requirements such as Zig or musl support.</p><p>This page documents the current v3 behavior. The built-in check/fix loader is also described for maintainers, but its PHP Attributes, callback signatures, and loader classes are not yet a stable third-party extension API.</p><h2 id="running-doctor" tabindex="-1">Running Doctor <a class="header-anchor" href="#running-doctor" aria-label="Permalink to “Running Doctor”">​</a></h2><p>Run Doctor explicitly after installing StaticPHP, changing the target/toolchain, or updating system build tools:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Pre-built binary</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./spc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Source installation</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bin/spc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span></code></pre></div><p>The fix policy is selected with <code>--auto-fix</code> (<code>-y</code>):</p><table tabindex="0"><thead><tr><th>Invocation</th><th>Current behavior</th></tr></thead><tbody><tr><td><code>spc doctor</code></td><td>Ask before each available automatic fix</td></tr><tr><td><code>spc doctor --auto-fix</code></td><td>Apply available fixes without prompting</td></tr><tr><td><code>spc doctor --auto-fix=never</code></td><td>Report the first unresolved failure and do not attempt a fix</td></tr></tbody></table><p>Doctor exits successfully only when every applicable check either passes, is skipped, or reports a fix that the current implementation considers successful. It stops at the first unresolved failure.</p><p>The <code>craft</code> command also runs Doctor by default. It uses the automatic-fix policy without prompting; set <code>craft-options.doctor: false</code> in <code>craft.yml</code> to disable that step.</p><h2 id="execution-model" tabindex="-1">Execution Model <a class="header-anchor" href="#execution-model" aria-label="Permalink to “Execution Model”">​</a></h2><p>The current execution sequence is:</p><ol><li><code>DoctorLoader</code> collects built-in and Registry-provided classes and scans their public methods for Doctor Attributes.</li><li>Checks are sorted by descending <code>level</code>.</li><li><code>#[OptionalCheck]</code>, <code>limit_os</code>, and <code>SPC_SKIP_DOCTOR_CHECK_ITEMS</code> remove checks that do not apply.</li><li>The check callback is called directly.</li><li><code>null</code> means skipped; <code>CheckResult::ok()</code> means passed; <code>CheckResult::fail()</code> describes a failure and may name a fix.</li><li>Depending on the selected fix policy, Doctor rejects, prompts for, or immediately invokes the named fix through <code>ApplicationContext</code>.</li></ol><p><code>checkAll()</code> is fail-fast: it does not collect all failures into a final report. A successful fixer is currently accepted without re-running its original check.</p><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>An invalid check return value is currently printed as “Skipped due to invalid return value” and treated as successful. This is an implementation limitation, not behavior extension authors should rely on.</p></div><h2 id="built-in-check-groups" tabindex="-1">Built-in Check Groups <a class="header-anchor" href="#built-in-check-groups" aria-label="Permalink to “Built-in Check Groups”">​</a></h2><p>The exact list is target-aware. The current core Registry provides these groups:</p><table tabindex="0"><thead><tr><th>Scope</th><th>Checks</th></tr></thead><tbody><tr><td>Common</td><td>At least one Registry is loaded; the host OS is Linux, macOS, or Windows</td></tr><tr><td>Linux</td><td>Distribution build-tool baseline, CMake 3.22+, <code>re2c</code> 1.0.3+, and Linux headers on musl distributions</td></tr><tr><td>macOS</td><td>Homebrew or MacPorts, required build commands, GNU Bison 3+, and the selected Homebrew/MacPorts LLVM variant</td></tr><tr><td>Windows target</td><td><code>vswhere</code>, Visual Studio C++ tools, Git <code>patch.exe</code>, managed MSYS2, <code>7za.exe</code>, and the initialized MSVC command environment</td></tr><tr><td>Unix target</td><td>StaticPHP-managed and functional <code>pkg-config</code></td></tr><tr><td>Zig toolchain</td><td>Installed StaticPHP <code>zig</code> tool package</td></tr><tr><td>musl target/toolchain</td><td>Required musl wrapper and, for the legacy <code>MuslToolchain</code>, the cross toolchain under <code>/usr/local/musl</code></td></tr></tbody></table><p>Package-specific tools declared through a package&#39;s <code>tools</code> field are resolved by <code>PackageInstaller</code>; they are not all duplicated as baseline Doctor checks. See <a href="./system-build-tools.html">Compilation Tools</a> for the platform package lists and toolchain details.</p><h2 id="automatic-fixes" tabindex="-1">Automatic Fixes <a class="header-anchor" href="#automatic-fixes" aria-label="Permalink to “Automatic Fixes”">​</a></h2><p>Depending on the failed item and platform, a fix may:</p><ul><li>Invoke <code>apt</code>, <code>apk</code>, <code>dnf</code>, <code>yum</code>, <code>pacman</code>, Homebrew, or MacPorts.</li><li>Use <code>sudo</code> for system-level installation.</li><li>Download and install a StaticPHP Tool or Artifact package.</li><li>Build a replacement tool such as <code>re2c</code>.</li><li>Install musl runtime or cross-toolchain files outside the workspace.</li></ul><p>Automatic fixes can therefore use the network and change the host system. The default interactive policy asks first; CI commonly uses either <code>--auto-fix</code> in a disposable environment or <code>--auto-fix=never</code> in a pre-provisioned image.</p><p>Fixers do not share a transaction or rollback mechanism. A fixer returning <code>true</code> is currently considered sufficient, even if the environment still fails the original condition. Re-run <code>spc doctor --auto-fix=never</code> after a repair when verification matters.</p><h2 id="doctor-lock" tabindex="-1">Doctor Lock <a class="header-anchor" href="#doctor-lock" aria-label="Permalink to “Doctor Lock”">​</a></h2><p>After all checks are considered successful, Doctor writes <code>.spc-doctor.lock</code> containing the current StaticPHP version. <code>craft</code> writes the same lock after its Doctor step succeeds.</p><p>The preferred locations are:</p><table tabindex="0"><thead><tr><th>Platform</th><th>Preferred path</th><th>Fallbacks</th></tr></thead><tbody><tr><td>Unix</td><td><code>$XDG_CACHE_HOME/.spc-doctor.lock</code>, or <code>$HOME/.cache/.spc-doctor.lock</code></td><td>System temporary directory, then the working directory</td></tr><tr><td>Windows</td><td><code>%LOCALAPPDATA%\\.spc-doctor.lock</code></td><td>System temporary directory, then the working directory</td></tr></tbody></table><p>Build/download commands that call <code>checkDoctorCache()</code> only use this lock to decide whether to show a warning. The lock does not skip an explicit <code>spc doctor</code> run and does not block a build.</p><p>The current fingerprint is only the StaticPHP version. A target, toolchain, Registry set, PATH, or operating-system change does not invalidate it automatically. Delete the lock or run Doctor explicitly after such a change.</p><h2 id="skipping-checks-and-warnings" tabindex="-1">Skipping Checks and Warnings <a class="header-anchor" href="#skipping-checks-and-warnings" aria-label="Permalink to “Skipping Checks and Warnings”">​</a></h2><p>Two environment variables have different purposes:</p><div class="language-ini"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Skip named Doctor items during an actual Doctor run (exact item names, comma-separated)</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">SPC_SKIP_DOCTOR_CHECK_ITEMS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;if cmake version &gt;= 3.22,if re2c version &gt;= 1.0.3&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Suppress the pre-build warning when the Doctor lock is missing or stale</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">SPC_SKIP_DOCTOR_CHECK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=1</span></span></code></pre></div><p><code>SPC_SKIP_DOCTOR_CHECK</code> does not remove checks from an explicit <code>spc doctor</code> invocation. <code>SPC_SKIP_DOCTOR_CHECK_ITEMS</code> does, and a skipped required check can allow an unusable environment to appear healthy, so it should be limited to controlled CI or debugging cases.</p><h2 id="current-internal-registration-model" tabindex="-1">Current Internal Registration Model <a class="header-anchor" href="#current-internal-registration-model" aria-label="Permalink to “Current Internal Registration Model”">​</a></h2><p>A Registry can currently point <code>doctor.psr-4</code> or <code>doctor.classes</code> at PHP classes. <code>DoctorLoader</code> constructs each class without constructor arguments and recognizes:</p><table tabindex="0"><thead><tr><th>Attribute</th><th>Current role</th></tr></thead><tbody><tr><td><code>#[CheckItem]</code></td><td>Registers a check name, optional OS limit, and numeric level; higher levels run first</td></tr><tr><td><code>#[OptionalCheck]</code></td><td>Supplies a class- or method-level callable that decides whether the check is present</td></tr><tr><td><code>#[FixItem]</code></td><td>Registers a string fix name used by a failed <code>CheckResult</code></td></tr></tbody></table><p>Check callbacks currently receive no DI context and return <code>null</code> or <code>CheckResult</code>. Fix callbacks are invoked through <code>ApplicationContext</code>, with <code>CheckResult</code>&#39;s raw <code>fix_params</code> array used as callback context. The <code>manual</code> property on <code>CheckItem</code> is not consumed by the current executor.</p>`,38)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};