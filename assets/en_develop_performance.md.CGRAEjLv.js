import{G as e,K as t,at as n,j as r}from"./chunks/framework.DrNIf5Lg.js";var i=JSON.parse(`{"title":"Performance","description":"","frontmatter":{},"headers":[],"relativePath":"en/develop/performance.md","filePath":"en/develop/performance.md"}`),a={name:`en/develop/performance.md`};function o(r,i,a,o,s,c){return n(),e(`div`,null,[...i[0]||=[t(`<h1 id="performance" tabindex="-1">Performance <a class="header-anchor" href="#performance" aria-label="Permalink to “Performance”">​</a></h1><p>This page explains how StaticPHP-built PHP performs, what the build already optimizes for you, what you can tune yourself, and where the project&#39;s performance research is heading. It is written for two audiences at once: users who build PHP with StaticPHP and want a fast, fit-for-purpose binary, and contributors who want to understand or extend the optimization work.</p><p>One caveat up front: no build configuration is fastest for every workload. Results depend on PHP version, CPU, toolchain, extensions, SAPI, libc, and runtime configuration. This page describes defaults and tradeoffs, not a universal recipe.</p><h2 id="what-you-get-by-default" tabindex="-1">What You Get by Default <a class="header-anchor" href="#what-you-get-by-default" aria-label="Permalink to “What You Get by Default”">​</a></h2><p>A default StaticPHP build is already an optimized build. Without passing any extra flags you get:</p><ul><li><strong><code>-O3</code> optimization</strong> for PHP and all dependency libraries, favoring runtime speed over compile time and code size.</li><li><strong>Dead code elimination</strong>: <code>-ffunction-sections -fdata-sections</code> combined with <code>--gc-sections</code> (Linux) or <code>-Wl,-dead_strip</code> (macOS) removes unused functions and data, keeping binaries smaller.</li><li><strong>Cheaper ELF symbol handling</strong> on Linux: <code>-fno-semantic-interposition</code> and <code>-fno-plt</code> let the compiler avoid indirection that only matters for interposable shared libraries.</li><li><strong>Link hardening</strong> on Linux: <code>relro</code>, <code>-z now</code>, <code>noexecstack</code>, <code>--as-needed</code>.</li><li><strong>Link-time code generation on Windows</strong>: StaticPHP rewrites the final CLI/CGI/micro/embed link rules to include <code>/LTCG</code>.</li><li><strong>A lean PHP configuration</strong>: only the SAPIs and extensions you request are built, and <code>--enable-re2c-cgoto</code> is enabled by default.</li><li><strong>Stripped deployed binaries</strong> with debug info preserved separately under <code>buildroot/debug/</code>. <code>--no-strip</code> keeps symbols <em>without</em> disabling optimization — a lesson learned from <a href="https://github.com/crazywhalecc/static-php-cli/issues/385" target="_blank" rel="noreferrer">issue #385</a>, where v2&#39;s <code>--no-strip</code> silently selected <code>-O0</code> and made one Laravel test about 3× slower.</li></ul><p>The authoritative values live in <code>config/env.ini</code> (<code>SPC_DEFAULT_CFLAGS</code>, <code>SPC_DEFAULT_LDFLAGS</code>, <code>SPC_CMD_VAR_PHP_MAKE_EXTRA_CFLAGS</code>, and their per-platform variants).</p><p>Frame pointers and <code>-g</code> are deliberately kept during compilation so profiling tools (perf, samply, Instruments) produce usable stacks; the deployed binary is stripped anyway, so this costs nothing at runtime.</p><h2 id="linux-choosing-linkage-and-libc" tabindex="-1">Linux: Choosing Linkage and libc <a class="header-anchor" href="#linux-choosing-linkage-and-libc" aria-label="Permalink to “Linux: Choosing Linkage and libc”">​</a></h2><p>The Linux default is a <strong>fully static musl binary</strong> (<code>SPC_TARGET=native-native-musl</code> with the Zig toolchain): a single file that runs on any compatible kernel, with no shared extensions and no FFI. That is the right choice for distribution, but it is a <em>portability</em> choice first and a performance choice second.</p><p>Three targets matter for performance work:</p><table tabindex="0"><thead><tr><th>Target</th><th>libc / linkage</th><th>Shared extensions, FFI</th><th>Choose when</th></tr></thead><tbody><tr><td><code>native-native-musl</code></td><td>musl, fully static</td><td>No</td><td>Default: portable single-file distribution</td></tr><tr><td><code>native-native-musl -dynamic</code></td><td>musl, dynamic</td><td>Yes</td><td>Experiments isolating linkage effects under musl</td></tr><tr><td><code>native-native-gnu.2.17</code></td><td>glibc, dynamic (2.17 baseline)</td><td>Yes</td><td>You need <code>.so</code> extensions, FFI, NSS, or your own benchmarks favor glibc</td></tr></tbody></table><p>&quot;Dynamic&quot; here only means libc and the loader are dynamic — StaticPHP still links most libraries and extensions statically into the executable. Only the fully static musl target cannot load extensions at runtime.</p><p><strong>Does linkage actually affect speed?</strong> Less than most people assume. Measurements in <a href="https://github.com/crazywhalecc/static-php-cli/issues/838" target="_blank" rel="noreferrer">issue #838</a> showed static and dynamic builds within about 1% of each other on PHP 8.4. Static linking can shave loader and relocation time off very short CLI invocations; long-running FPM or FrankenPHP workers amortize that completely. Likewise, musl-versus-glibc differences only appear where a workload actually reaches libc — the allocator under heavy native allocation, threading under ZTS, DNS resolution, locale/iconv. Pure PHP code runs in the Zend VM and Zend Memory Manager, which mostly bypass the system allocator.</p><p>Practical guidance:</p><ul><li><strong>Default to musl static.</strong> It is portable, predictable, and not measurably slower for typical PHP workloads.</li><li><strong>Choose glibc dynamic</strong> when you need shared extensions or FFI, or when your own benchmarks show a glibc advantage (glibc&#39;s tunable per-thread allocator caches can help allocation-heavy threaded workloads).</li><li><strong>Benchmark with your real application.</strong> Resolver behavior alone (musl queries nameservers in parallel; glibc traditionally sequentially) can dominate a &quot;libc performance&quot; comparison if your app does many DNS lookups.</li></ul><p>If you publish or compare numbers, verify linkage from the binary itself rather than filenames:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> buildroot/bin/php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">readelf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> buildroot/bin/php</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Requesting program interpreter&#39;</span></span></code></pre></div><h2 id="toolchains-and-php-version" tabindex="-1">Toolchains and PHP Version <a class="header-anchor" href="#toolchains-and-php-version" aria-label="Permalink to “Toolchains and PHP Version”">​</a></h2><p>Compiler choice can matter more than any individual flag:</p><ul><li><strong>Linux defaults to Zig</strong> (<code>zig cc</code>, LLVM-based) because it provides flexible cross-targeting and reproducible musl/glibc selection — not because it always produces the fastest binary. Native GCC remains available as a maintainer-level option via <code>SPC_TOOLCHAIN</code>.</li><li><strong>GCC vs Clang</strong>: <a href="https://github.com/crazywhalecc/static-php-cli/issues/838" target="_blank" rel="noreferrer">issue #838</a> found large differences for some PHP 8.4 x86-64 tests because the older Zend VM can exploit GCC&#39;s global register variables. PHP 8.5&#39;s newer VM changes this picture — conclusions from one PHP version do not transfer automatically to the next.</li><li><strong>macOS</strong> uses system Clang by default; <code>SPC_USE_LLVM=brew</code> or <code>=port</code> selects a newer upstream LLVM from Homebrew or MacPorts as a coherent toolchain. This frees builds from Apple&#39;s release cycle and helps some PHP versions and workloads, but it is not a guaranteed uplift.</li><li><strong>Windows</strong> uses MSVC with <code>/LTCG</code> on the final link. FrankenPHP&#39;s CGO link goes through Clang/LLD, so some libraries deliberately avoid <code>/GL</code> — <code>/LTCG</code> on the link line does not mean every object participates in whole-program optimization.</li></ul><p>The compiler landscape per PHP version is one of the most underappreciated performance factors: upgrading PHP (or the compiler) can outperform weeks of flag tuning.</p><h2 id="tuning-your-own-build" tabindex="-1">Tuning Your Own Build <a class="header-anchor" href="#tuning-your-own-build" aria-label="Permalink to “Tuning Your Own Build”">​</a></h2><p>All defaults can be overridden reproducibly through <code>config/env.custom.ini</code> (or process environment variables). Values replace the complete default string, so you keep the baseline visible and change one factor at a time.</p><h3 id="cpu-instruction-set-level" tabindex="-1">CPU instruction set level <a class="header-anchor" href="#cpu-instruction-set-level" aria-label="Permalink to “CPU instruction set level”">​</a></h3><p>The most impactful user-level knob on Linux is the ISA baseline. Adding <code>-march=x86-64-v3</code> lets the compiler use AVX2 and friends:</p><div class="language-ini"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">[linux]</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">SPC_DEFAULT_CFLAGS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-fPIC -O3 -pipe -fno-plt -fno-semantic-interposition -fstack-clash-protection -fno-omit-frame-pointer -mno-omit-leaf-frame-pointer -ffunction-sections -fdata-sections -march=x86-64-v3&quot;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">SPC_DEFAULT_CXXFLAGS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;\${SPC_DEFAULT_CFLAGS}&quot;</span></span></code></pre></div><p>The tradeoff is portability: the binary will not run on CPUs below that level, and <code>-march=native</code> ties it to the build machine. <a href="https://github.com/crazywhalecc/static-php-cli/issues/1088" target="_blank" rel="noreferrer">Issue #1088</a> tracks a friendlier way to declare intrinsic levels across libraries and PHP instead of hand-managing flags.</p><h3 id="scope-of-overrides" tabindex="-1">Scope of overrides <a class="header-anchor" href="#scope-of-overrides" aria-label="Permalink to “Scope of overrides”">​</a></h3><ul><li><strong>Everything</strong>: <code>SPC_DEFAULT_CFLAGS</code> / <code>SPC_DEFAULT_CXXFLAGS</code> / <code>SPC_DEFAULT_LDFLAGS</code></li><li><strong>PHP and in-tree extensions only</strong>: <code>SPC_CMD_VAR_PHP_MAKE_EXTRA_CFLAGS</code> (and CXX/LD variants)</li><li><strong>One library</strong>: snake-case variables such as <code>libaom_CFLAGS</code>, merged with the defaults by the common build executors</li></ul><h3 id="remember-many-build-performance-questions-are-runtime-questions" tabindex="-1">Remember: many &quot;build performance&quot; questions are runtime questions <a class="header-anchor" href="#remember-many-build-performance-questions-are-runtime-questions" aria-label="Permalink to “Remember: many &quot;build performance&quot; questions are runtime questions”">​</a></h3><ul><li><strong>Opcache is a runtime feature.</strong> Building <code>ext-opcache</code> only makes it available; <code>opcache.enable_cli</code>, JIT settings, and real application behavior determine what it does for you. <code>--disable-opcache-jit</code> changes build capability, not measured speed.</li><li><strong>ZTS vs NTS</strong>: FrankenPHP requires ZTS; everything else defaults to NTS. Historical tests showed small differences, specific to those workloads.</li><li><strong>FrankenPHP</strong> performance is dominated by worker mode and count, application boot behavior, Caddy modules, and Go runtime settings — no compiler flag fixes an unrepresentative server configuration.</li><li><strong>UPX</strong> (<code>--with-upx-pack</code>) is a size optimization that trades startup time, memory mapping, and security-tool behavior. It does not make PHP execute faster.</li></ul><h2 id="ongoing-research" tabindex="-1">Ongoing Research <a class="header-anchor" href="#ongoing-research" aria-label="Permalink to “Ongoing Research”">​</a></h2><p>Performance work in StaticPHP is active and incremental. The main directions:</p><p><strong>PGO (Profile-Guided Optimization)</strong> — <a href="https://github.com/crazywhalecc/static-php-cli/pull/1138" target="_blank" rel="noreferrer">PR #1138</a> is bringing v3-native PGO: instrument the build, train it on representative workloads, rebuild with the collected profiles. The hard parts are orchestration, not flags: per-SAPI profile directories, clean rebuilds when switching profiles, shutdown patches so profiles actually flush (Go/CGO processes bypass the libc <code>atexit</code> path), and invalidation when sources change. A naive <code>-fprofile-generate</code> in the global flags instruments every dependency library and may not even produce usable data, which is why PGO will arrive as a structured feature rather than an environment variable. Related: <a href="https://github.com/crazywhalecc/static-php-cli/pull/1142" target="_blank" rel="noreferrer">PR #1142</a> already uses FrankenPHP&#39;s bundled Go profile (<code>default.pgo</code>) for the Go/Caddy portion — that optimizes Go code only, not php-src against your application. The earlier one-size-fits-all proposal (<a href="https://github.com/crazywhalecc/static-php-cli/issues/862" target="_blank" rel="noreferrer">issue #862</a>) was closed because meaningful training has to be SAPI-aware and user-owned.</p><p><strong>LTO (Link-Time Optimization)</strong> — not enabled by default on Unix. In one environment measured in <a href="https://github.com/crazywhalecc/static-php-cli/issues/838" target="_blank" rel="noreferrer">issue #838</a>, the runtime gain was about 2% while ThinLTO doubled build time and full LTO multiplied it by seven. LTO also exposes archive incompatibilities, package bugs, and CGO linker limitations across dozens of dependency libraries, so the current direction is limited-scope LTO (PHP core and in-tree extensions, one toolchain family, consistent <code>-flto=thin</code>) rather than a global switch. Windows already uses <code>/LTCG</code> on final links as noted above.</p><p><strong>Better flag plumbing</strong> — flags propagate through <code>env.ini</code> → toolchain → package executors → PHP/FrankenPHP targets, but packages with hand-written compiler commands can still ignore parts of the global set. Making propagation uniform is ongoing work that benefits every other optimization.</p><h2 id="how-we-benchmark" tabindex="-1">How We Benchmark <a class="header-anchor" href="#how-we-benchmark" aria-label="Permalink to “How We Benchmark”">​</a></h2><p>Performance claims in this project follow a reproducible methodology, centered on <a href="https://github.com/crazywhalecc/static-php-cli/issues/838" target="_blank" rel="noreferrer">issue #838</a> as the public notebook:</p><ul><li>Compare builds that differ in <strong>one factor</strong>, holding PHP version, commit, extensions, SAPI, ZTS/NTS, INI, libc, and compiler constant.</li><li>Alternate baseline and candidate on the <strong>same machine</strong>, with repetitions; medians and variation matter more than a single best run.</li><li>Record full context: toolchain versions, <code>SPC_TARGET</code>, CPU model and frequency policy, extension set, env overrides, workload revision, concurrency, warmup, plus throughput/tail-latency/RSS/binary-size/build-time as applicable.</li><li>Verify what was actually built (linkage via <code>file</code>/<code>readelf</code>, flags via build logs and <code>php -i</code>) rather than trusting invocation parameters.</li></ul><p>If you report a performance issue or regression, including this context makes it actionable.</p><h2 id="history-and-lessons" tabindex="-1">History and Lessons <a class="header-anchor" href="#history-and-lessons" aria-label="Permalink to “History and Lessons”">​</a></h2><table tabindex="0"><thead><tr><th>Record</th><th>Lesson</th></tr></thead><tbody><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/issues/385" target="_blank" rel="noreferrer">Issue #385</a></td><td>Debug symbols must never silently disable optimization; led to customizable PHP compiler variables.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/pull/806" target="_blank" rel="noreferrer">PR #806</a></td><td>Zig toolchain added for target flexibility; discussion contains the static/dynamic ~1% observation.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/issues/838" target="_blank" rel="noreferrer">Issue #838</a></td><td>The main performance notebook: compiler, ZTS/NTS, Opcache, LTO, VM, and architecture comparisons behind the <code>-O3</code> defaults.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/issues/985" target="_blank" rel="noreferrer">Issue #985</a></td><td>v3 toolchain decision: predictable defaults first, performance-oriented alternatives available.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/issues/862" target="_blank" rel="noreferrer">Issue #862</a></td><td>A universal PGO training script is unrealistic; training must be SAPI-aware and user-owned.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/pull/966" target="_blank" rel="noreferrer">PR #966</a></td><td>Unified PHP make flags; decoupled stripping from optimization.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/pull/1142" target="_blank" rel="noreferrer">PR #1142</a></td><td>FrankenPHP&#39;s bundled Go profile used in the build; distinct from application-trained PGO.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/issues/1088" target="_blank" rel="noreferrer">Issue #1088</a></td><td>Open: declaring CPU intrinsic levels uniformly across libraries and PHP.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/pull/1138" target="_blank" rel="noreferrer">PR #1138</a></td><td>v3-native PGO implementation in progress.</td></tr><tr><td><a href="https://github.com/crazywhalecc/static-php-cli/pull/1150" target="_blank" rel="noreferrer">PR #1150</a></td><td>Optimization flags must be target-format aware (an ELF-only flag broke macOS configure checks).</td></tr></tbody></table><h2 id="contributing" tabindex="-1">Contributing <a class="header-anchor" href="#contributing" aria-label="Permalink to “Contributing”">​</a></h2><p>Most of the performance investigation and implementation has been led by <a href="https://github.com/henderkes" target="_blank" rel="noreferrer">@henderkes</a>, with review, integration, and testing from other contributors — and more help is welcome:</p><ul><li><strong>Benchmarks</strong>: reproducible measurements on real applications (especially FrankenPHP/FPM under load, allocation-heavy extensions, DNS-heavy workloads) are always valuable. Follow the methodology above and share results in <a href="https://github.com/crazywhalecc/static-php-cli/issues/838" target="_blank" rel="noreferrer">issue #838</a> or a new issue.</li><li><strong>Platform coverage</strong>: ARM servers, older glibc baselines, and Windows/MSVC behavior are less explored than Linux x86_64.</li><li><strong>PGO and LTO</strong>: testing <a href="https://github.com/crazywhalecc/static-php-cli/pull/1138" target="_blank" rel="noreferrer">PR #1138</a> on your workloads and reporting build or runtime issues directly shapes the feature.</li></ul><p>Because StaticPHP sits at the intersection of PHP, its dependency ecosystem, and multiple toolchains, findings here often surface upstream bugs — in php-src, in libraries, in Zig or LLVM. Reports and fixes that flow back upstream improve things for the wider PHP community, not just for static builds.</p>`,47)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};