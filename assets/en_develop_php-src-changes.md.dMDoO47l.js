import{G as e,K as t,at as n,j as r}from"./chunks/framework.DrNIf5Lg.js";var i=JSON.parse(`{"title":"PHP Source Modifications","description":"","frontmatter":{},"headers":[],"relativePath":"en/develop/php-src-changes.md","filePath":"en/develop/php-src-changes.md"}`),a={name:`en/develop/php-src-changes.md`};function o(r,i,a,o,s,c){return n(),e(`div`,null,[...i[0]||=[t(`<h1 id="php-source-modifications" tabindex="-1">PHP Source Modifications <a class="header-anchor" href="#php-source-modifications" aria-label="Permalink to “PHP Source Modifications”">​</a></h1><p>StaticPHP modifies the extracted PHP source tree to support static linking, additional SAPIs, older PHP branches, and platform-specific toolchains. These changes are applied to the generated workspace under <code>source/php-src</code>; the downloaded archive in <code>downloads/</code> is not modified.</p><p>This page describes the patches shipped by the <code>core</code> registry. It is an implementation inventory for maintainers, not a promise that <code>SourcePatcher</code>, lifecycle callback signatures, or individual patch names are stable public APIs.</p><h2 id="when-modifications-are-applied" tabindex="-1">When Modifications Are Applied <a class="header-anchor" href="#when-modifications-are-applied" aria-label="Permalink to “When Modifications Are Applied”">​</a></h2><p>Changes enter the source tree at several points:</p><ol><li><strong>After <code>php-src</code> extraction</strong>: artifact hooks apply compatibility changes before any package is built.</li><li><strong>Before <code>buildconf</code> / configure</strong>: the PHP target applies the versioned patch set and rewrites build-system inputs.</li><li><strong>Before an individual SAPI or extension build</strong>: conditional hooks patch generated Makefiles or extension sources.</li><li><strong>Temporarily around a stage</strong>: a small number of changes are backed up and restored after that SAPI has been built.</li></ol><p>Methods annotated with <code>#[PatchDescription]</code> emit a <code>[PATCH]</code> message when StaticPHP invokes them through its lifecycle/DI dispatcher. This is useful for tracing a build, but it is not a complete machine-readable manifest: direct method calls do not pass through that logger, and a few source rewrites and generated-file changes are not annotated.</p><h2 id="changes-applied-after-extraction" tabindex="-1">Changes Applied After Extraction <a class="header-anchor" href="#changes-applied-after-extraction" aria-label="Permalink to “Changes Applied After Extraction”">​</a></h2><p><code>src/Package/Artifact/php_src.php</code> runs whenever a fresh <code>php-src</code> artifact is extracted.</p><table tabindex="0"><thead><tr><th>Source or condition</th><th>Modification</th></tr></thead><tbody><tr><td>PHP older than 8.0</td><td>Applies the legacy stream-cast compatibility patch used by older Alpine/musl builds</td></tr><tr><td>PHP 8.0.x</td><td>Applies the libxml2 2.12 compatibility patch to DOM, libxml, and SOAP, plus the legacy stream-cast patch</td></tr><tr><td>PHP 8.1.x</td><td>Applies the legacy stream-cast patch</td></tr><tr><td>GD on all supported PHP branches</td><td>Replaces <code>ext/gd/config.w32</code> with StaticPHP&#39;s version-specific static-build configuration; PHP older than 8.2 also receives the <code>gdft.c</code> <code>_WIN32</code> compatibility fix</td></tr><tr><td>Bundled IMAP source without a license file</td><td>Adds the bundled Apache license text as <code>ext/imap/LICENSE</code> for license collection</td></tr></tbody></table><p>The GD and IMAP operations edit files that only matter when those extensions are present or built. The extraction hooks themselves run as part of extracting PHP source, before extension resolution reaches the build stage.</p><h2 id="versioned-php-patch-set" tabindex="-1">Versioned PHP Patch Set <a class="header-anchor" href="#versioned-php-patch-set" aria-label="Permalink to “Versioned PHP Patch Set”">​</a></h2><p>Before <code>buildconf</code>, both Unix and Windows builds call <code>SourcePatcher::patchPhpSrc()</code>. Despite its historical “micro patches” name, this happens for the PHP build in general; selecting <code>php-micro</code> is not required.</p><p>The default <code>SPC_MICRO_PATCHES</code> values from <code>config/env.ini</code> are:</p><table tabindex="0"><thead><tr><th>Host platform</th><th>Default patch names</th></tr></thead><tbody><tr><td>Linux</td><td><code>cli_checks</code>, <code>disable_huge_page</code></td></tr><tr><td>macOS</td><td><code>cli_checks</code>, <code>macos_iconv</code></td></tr><tr><td>Windows</td><td><code>static_extensions_win32</code>, <code>cli_checks</code>, <code>disable_huge_page</code>, <code>vcruntime140</code>, <code>win32</code>, <code>zend_stream</code>, <code>cli_static</code>, <code>win32_api</code></td></tr></tbody></table><p>Their current purposes are:</p><table tabindex="0"><thead><tr><th>Patch</th><th>Purpose</th></tr></thead><tbody><tr><td><code>cli_checks</code></td><td>Treat the micro SAPI like CLI in PHP code paths that otherwise compare the SAPI name with <code>cli</code></td></tr><tr><td><code>disable_huge_page</code></td><td>Disable the Linux 2 MiB segment-alignment probe, avoiding unnecessary growth of SFX binaries</td></tr><tr><td><code>macos_iconv</code></td><td>Make the PHP iconv check link with the macOS iconv library correctly</td></tr><tr><td><code>static_extensions_win32</code></td><td>Adjust Windows extension configuration, including Fileinfo and OpenSSL, for static builds</td></tr><tr><td><code>cli_static</code></td><td>Adjust the Windows CLI source for a fully static executable</td></tr><tr><td><code>vcruntime140</code></td><td>Bypass the startup check for a dynamically loaded <code>vcruntime140</code> module</td></tr><tr><td><code>win32</code></td><td>Switch the PHP Windows build configuration from the dynamic MSVC runtime (<code>/MD</code>) to the static runtime (<code>/MT</code>)</td></tr><tr><td><code>zend_stream</code></td><td>Avoid a conflicting <code>isatty</code> declaration with the static Windows CRT</td></tr><tr><td><code>win32_api</code></td><td>Correct Windows API declarations needed by supported PHP branches</td></tr></tbody></table><p>Patch files live in <code>src/globals/patch/php-src-patches/</code>. A name may have an unversioned file such as <code>zend_stream.patch</code>, or versioned files such as <code>cli_checks_84.patch</code>. For a versioned name, StaticPHP chooses the newest available PHP minor less than or equal to the source minor. For example, an 8.2 source uses an <code>_81</code> patch when <code>_81</code> and <code>_84</code> are available but <code>_82</code> is not. PHP 7.4 skips this patch-set loader.</p><p>The directory also contains non-default or conditionally selected patches such as <code>comctl32</code>, <code>phar</code>, and <code>static_opcache</code>. A missing compatible patch or a failed <code>patch -p1</code> application stops the build with <code>PatchException</code>.</p><h2 id="common-build-time-changes" tabindex="-1">Common Build-Time Changes <a class="header-anchor" href="#common-build-time-changes" aria-label="Permalink to “Common Build-Time Changes”">​</a></h2><p>The PHP target applies two changes independently of the platform-specific tables below.</p><h3 id="staticphp-version-metadata" tabindex="-1">StaticPHP Version Metadata <a class="header-anchor" href="#staticphp-version-metadata" aria-label="Permalink to “StaticPHP Version Metadata”">​</a></h3><p>Before the PHP <code>build</code> stage, StaticPHP adds an INI entry named <code>StaticPHP.version</code> to <code>main/main.c</code> when the source does not already contain it. This makes the StaticPHP version that produced the binary available at runtime.</p><h3 id="hardcoded-ini-values" tabindex="-1">Hardcoded INI Values <a class="header-anchor" href="#hardcoded-ini-values" aria-label="Permalink to “Hardcoded INI Values”">​</a></h3><p>Each <code>--with-hardcoded-ini=key=value</code> option adds a literal line to <code>HARDCODED_INI</code> in every available supported SAPI source:</p><ul><li><code>sapi/cli/php_cli.c</code></li><li><code>sapi/micro/php_micro.c</code></li><li><code>sapi/embed/php_embed.c</code></li></ul><p>The original files are saved with a <code>.bak</code> suffix. When a later invocation supplies another non-empty set, StaticPHP restores the backups before applying that set, so replacing one configured set with another does not accumulate old values. Completely omitting all <code>--with-hardcoded-ini</code> options currently does not run the restore path; re-extract <code>php-src</code> to remove previously injected values.</p><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>Hardcoded values become part of the source and resulting binary. Do not use this option for passwords, tokens, or other secrets.</p></div><h2 id="unix-build-time-changes" tabindex="-1">Unix Build-Time Changes <a class="header-anchor" href="#unix-build-time-changes" aria-label="Permalink to “Unix Build-Time Changes”">​</a></h2><p>Linux and macOS builds apply the following target-aware changes:</p><table tabindex="0"><thead><tr><th>Condition</th><th>File or generated input</th><th>Modification</th></tr></thead><tbody><tr><td>Every Unix PHP build</td><td><code>configure.ac</code></td><td>Replaces host <code>ldd</code>-based musl detection with the libc selected by <code>SPC_TARGET</code>; the original file is backed up</td></tr><tr><td>Every Unix PHP build</td><td><code>build/php.m4</code> and extension <code>config.m4</code> files</td><td>Replaces <code>PKG_CHECK_MODULES</code> with <code>PKG_CHECK_MODULES_STATIC</code> so dependency checks include private static link requirements</td></tr><tr><td>PHP 8.3.x</td><td><code>build/php.m4</code></td><td>Applies the AVX-512 configure-cache compatibility patch used before PHP 8.4</td></tr><tr><td>PHP older than 8.3</td><td>generated <code>configure</code></td><td>Uses the supported <code>-std=gnu17</code> probe instead of the newer <code>-std=gnu23</code> probe</td></tr><tr><td>Dynamically linked musl target</td><td><code>TSRM/TSRM.h</code></td><td>Gives the main TSRM TLS cache symbol default visibility so shared extensions using the <code>initial-exec</code> TLS model can load</td></tr><tr><td>Linux</td><td>generated <code>Makefile</code></td><td>Normalizes accidental <code>//lib</code> paths to <code>/lib</code></td></tr><tr><td>Linux with Zig</td><td>generated <code>Makefile</code></td><td>Uses the host <code>cc</code> for <code>BUILD_CC</code> instead of <code>zig-cc</code>, preventing host tools such as minilua from being built for the target environment</td></tr><tr><td>A release linker flag is present</td><td><code>ext/standard/info.c</code></td><td>Hides the configure command from PHP information output</td></tr></tbody></table><p>When <code>php-micro</code> is resolved, its package hooks also adjust the generated Makefile so the embed prerequisite builds only <code>libphp.la</code> and the main PHP install step does not install the micro binary prematurely.</p><p>If <code>ext-phar</code> is selected for a Unix micro build, <code>ext/phar/phar.c</code> is temporarily changed so a compressed PHAR appended to the current executable can be recognized even when the executable name does not contain <code>.phar</code>. The original file is restored immediately after the micro stage.</p><h2 id="windows-build-time-changes" tabindex="-1">Windows Build-Time Changes <a class="header-anchor" href="#windows-build-time-changes" aria-label="Permalink to “Windows Build-Time Changes”">​</a></h2><p>Windows requires more extensive build-system changes because PHP&#39;s upstream Windows flow normally assumes a dynamic CRT, PHP SDK tools, and import-library-based SAPIs.</p><table tabindex="0"><thead><tr><th>Stage or condition</th><th>Modification</th></tr></thead><tbody><tr><td>Before <code>buildconf.bat</code></td><td>Removes the shared <code>dllmain.c</code> object from the relevant Windows core build rule, then applies the versioned patch set</td></tr><tr><td>Missing <code>win32/wsyslog.h</code></td><td>Generates a compatibility header with the event IDs defined by <code>win32/build/wsyslog.mc</code></td></tr><tr><td>PHP 8.1.x</td><td>Moves the Fiber assembly objects from linker flags to the assembly object list so they participate in static linking</td></tr><tr><td>Detected Visual Studio</td><td>Rewrites <code>win32/build/confutils.js</code> so supported Visual Studio installations report the correct PHP toolset name</td></tr><tr><td>After <code>buildconf.bat</code></td><td>Disables the generated <code>configure.js</code> PHP SDK version check because v3 uses MSVC plus its managed MSYS2 environment instead of PHP SDK binary tools</td></tr><tr><td><code>--enable-micro-win32</code></td><td>Defines <code>PHP_MICRO_WIN32_NO_CONSOLE</code> in <code>sapi/micro/php_micro.c</code>; a backup is restored when the option is no longer active</td></tr><tr><td>CLI build</td><td>Rewrites the generated <code>php.exe</code> rule to link PHP core, the CLI SAPI, static extensions, assembly objects, and static libraries directly; also places <code>buildroot/include</code> flags before extension flags</td></tr><tr><td>CGI build</td><td>Rewrites the generated <code>php-cgi.exe</code> rule for static linking and removes the duplicate <code>ZEND_TSRMLS_CACHE_DEFINE()</code> from the CGI source</td></tr><tr><td>Micro build</td><td>Adds the Fiber assembly prerequisites needed by the generated micro target and supplies the static library set</td></tr><tr><td>Embed build</td><td>Rewrites the generated embed target so <code>phpNembed.lib</code> (for example, <code>php8embed.lib</code>) contains the embed SAPI, PHP core, static extensions, and assembly objects instead of being only an import library</td></tr></tbody></table><p>The final embed change is also what makes the Windows FrankenPHP build possible: FrankenPHP links the self-contained embed library through CGO with Clang/LLD. Unix FrankenPHP does not apply a separate php-src patch; it consumes the Unix <code>php-embed</code> library produced by the normal PHP target.</p><h2 id="conditional-extension-patches" tabindex="-1">Conditional Extension Patches <a class="header-anchor" href="#conditional-extension-patches" aria-label="Permalink to “Conditional Extension Patches”">​</a></h2><p>Extension package classes may modify their source under <code>php-src/ext/&lt;name&gt;</code> when that extension is resolved. Two cross-cutting examples are:</p><ul><li><strong>Opcache</strong>: <code>ext-opcache</code> applies a version-specific static Opcache patch for PHP 8.0–8.4.x. It uses dedicated legacy patches before PHP 8.2.23 and PHP 8.3.11, and otherwise uses the versioned <code>static_opcache</code> patch. PHP 8.5 and later support the required static path without this patch.</li><li><strong>PHAR for micro</strong>: the Unix micro hook described above is applied only when <code>ext-phar</code> participates in the build and is reversed after the micro binary is produced.</li></ul><p>Other extension classes contain narrower compatibility changes for particular PHP, compiler, dependency-library, or operating-system versions. They are deliberately kept next to that extension&#39;s build logic in <code>src/Package/Extension/</code>, rather than added to the global PHP patch set. Search for <code>#[PatchDescription]</code> to review the current annotated inventory.</p><h2 id="patch-application-and-recovery" tabindex="-1">Patch Application and Recovery <a class="header-anchor" href="#patch-application-and-recovery" aria-label="Permalink to “Patch Application and Recovery”">​</a></h2><p><code>SourcePatcher::patchFile()</code> resolves relative patch names under <code>src/globals/patch/</code>, checks whether the patch is already applied with a reverse dry run, and then invokes <code>patch --binary -p1</code>. This makes file-based patches safe to encounter again in the same extracted tree. Direct string replacements are written so repeated invocation normally finds no original text to replace.</p><p>There is no transaction that restores the whole PHP tree after a build. Only explicitly temporary changes, such as the PHAR and hardcoded-INI backups, have dedicated restoration logic. To return to pristine upstream source, remove/re-extract <code>source/php-src</code>, or use <code>switch-php-version</code> without <code>--keep-source</code> when changing or refreshing the PHP version.</p><p>The v2 arbitrary patch-point injection mechanism is not a supported v3 interface. Until a public patch extension contract is defined, new core patches should be implemented as maintainer-owned artifact or package hooks.</p><h2 id="adding-or-updating-a-core-patch" tabindex="-1">Adding or Updating a Core Patch <a class="header-anchor" href="#adding-or-updating-a-core-patch" aria-label="Permalink to “Adding or Updating a Core Patch”">​</a></h2><p>When maintaining a built-in patch:</p><ol><li>Prefer an upstream fix; keep a downstream patch only while supported PHP branches still need it.</li><li>Put reusable patch files in <code>src/globals/patch/</code>; put the version-selected micro/static-PHP set in <code>src/globals/patch/php-src-patches/</code>.</li><li>Use an exact PHP/platform/dependency condition so unrelated builds do not mutate their sources unnecessarily.</li><li>Make the operation repeatable and fail loudly when the expected upstream context is no longer present.</li><li>Add <code>#[PatchDescription]</code> to a method invoked through the lifecycle/DI dispatcher so the change is visible in build logs; log direct calls explicitly.</li><li>Test the oldest and newest PHP branches affected by the patch, and update both language versions of this page when behavior changes.</li></ol>`,48)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};