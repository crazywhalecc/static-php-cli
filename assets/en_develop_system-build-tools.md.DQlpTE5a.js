import{G as e,K as t,at as n,j as r}from"./chunks/framework.DrNIf5Lg.js";var i=JSON.parse(`{"title":"Compilation Tools","description":"","frontmatter":{},"headers":[],"relativePath":"en/develop/system-build-tools.md","filePath":"en/develop/system-build-tools.md"}`),a={name:`en/develop/system-build-tools.md`};function o(r,i,a,o,s,c){return n(),e(`div`,null,[...i[0]||=[t(`<h1 id="compilation-tools" tabindex="-1">Compilation Tools <a class="header-anchor" href="#compilation-tools" aria-label="Permalink to “Compilation Tools”">​</a></h1><p>StaticPHP builds PHP, libraries, extensions, and helper tools by invoking native build systems. The environment is split into three layers:</p><ol><li><strong>Host tools</strong> such as <code>autoconf</code>, <code>cmake</code>, <code>make</code>, <code>patch</code>, and parsers/code generators.</li><li><strong>Compiler toolchain</strong> such as Zig, GCC, Clang, or MSVC. The selected toolchain provides the compiler, linker, archiver, and target-specific environment.</li><li><strong>Package build executors</strong> that apply StaticPHP&#39;s common Autoconf or CMake arguments and run the package build.</li></ol><p>This distinction matters when troubleshooting: having <code>cmake</code> installed does not mean that the selected compiler toolchain is initialized, and having a compiler does not mean that all source-generation tools are present.</p><h2 id="checking-the-environment" tabindex="-1">Checking the Environment <a class="header-anchor" href="#checking-the-environment" aria-label="Permalink to “Checking the Environment”">​</a></h2><p>Run <code>doctor</code> after installing StaticPHP and after changing the build target or toolchain:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Pre-built binary</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./spc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># Source installation</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bin/spc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span></code></pre></div><p>To let StaticPHP install supported missing items, use:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./spc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --auto-fix</span></span></code></pre></div><p>On Linux and macOS, automatic fixes may invoke the system package manager and may require <code>sudo</code>. On Windows, StaticPHP can install its own helper packages, but it cannot install Visual Studio or Git for you. See the <a href="/en/guide/cli-reference.html#doctor"><code>doctor</code> command reference</a> for its command-line options.</p><p>Doctor checks are target-aware. For example, Zig and musl-related checks only run when the selected toolchain and target require them, and a Homebrew or MacPorts LLVM check only runs when that LLVM variant is selected.</p><h2 id="what-the-tools-are-used-for" tabindex="-1">What the Tools Are Used For <a class="header-anchor" href="#what-the-tools-are-used-for" aria-label="Permalink to “What the Tools Are Used For”">​</a></h2><table tabindex="0"><thead><tr><th>Tool group</th><th>Examples</th><th>Role in a build</th></tr></thead><tbody><tr><td>Compiler and binutils</td><td><code>zig</code>, <code>gcc</code>, <code>g++</code>, <code>clang</code>, <code>cl.exe</code>, <code>ld</code>, <code>link.exe</code>, <code>ar</code>, <code>lib.exe</code>, <code>ranlib</code></td><td>Compile and link PHP and dependency libraries</td></tr><tr><td>Autotools</td><td><code>autoconf</code>, <code>automake</code>, <code>libtoolize</code> / <code>glibtoolize</code>, <code>autopoint</code></td><td>Generate <code>configure</code> scripts and supporting files</td></tr><tr><td>Make tools</td><td><code>make</code>, <code>nmake.exe</code></td><td>Execute generated Makefiles</td></tr><tr><td>CMake</td><td><code>cmake</code></td><td>Configure and build CMake-based packages</td></tr><tr><td>Source generators</td><td><code>bison</code>, <code>re2c</code>, <code>flex</code>, <code>gperf</code></td><td>Regenerate parsers, lexers, and generated C/C++ sources</td></tr><tr><td>Dependency metadata</td><td><code>pkg-config</code></td><td>Read <code>.pc</code> files and return static compile/link flags</td></tr><tr><td>Source and archive tools</td><td><code>git</code>, <code>patch</code>, <code>tar</code>, <code>unzip</code>, <code>gzip</code>, <code>bzip2</code>, <code>xz</code>, <code>7za.exe</code></td><td>Fetch, patch, and extract source archives</td></tr><tr><td>Package-specific tools</td><td><code>nasm</code>, Perl, Go</td><td>Build packages such as OpenSSL or FrankenPHP when selected</td></tr></tbody></table><p>Not every package uses every tool. Doctor verifies the common host baseline and target-aware prerequisites; an individual package may also resolve a tool package such as Go, NASM, or Perl when selected.</p><h2 id="linux" tabindex="-1">Linux <a class="header-anchor" href="#linux" aria-label="Permalink to “Linux”">​</a></h2><p><code>LinuxToolCheck</code> detects the distribution from <code>/etc/os-release</code>, selects a package-family baseline, and checks the command (or file) provided by each package. Its current baselines are:</p><table tabindex="0"><thead><tr><th>Distribution family</th><th>Checked packages or commands</th></tr></thead><tbody><tr><td>Alpine</td><td><code>make</code>, <code>bison</code>, <code>re2c</code>, <code>flex</code>, <code>gperf</code>, <code>git</code>, <code>autoconf</code>, <code>automake</code>, <code>gettext-dev</code>, <code>tar</code>, <code>unzip</code>, <code>gzip</code>, <code>bzip2</code>, <code>cmake</code>, <code>gcc</code>, <code>g++</code>, <code>patch</code>, <code>binutils-gold</code>, <code>libtoolize</code>, <code>which</code></td></tr><tr><td>Debian / Ubuntu and the fallback baseline</td><td><code>make</code>, <code>bison</code>, <code>re2c</code>, <code>flex</code>, <code>gperf</code>, <code>git</code>, <code>autoconf</code>, <code>automake</code>, <code>autopoint</code>, <code>tar</code>, <code>unzip</code>, <code>gzip</code>, <code>gcc</code>, <code>g++</code>, <code>bzip2</code>, <code>cmake</code>, <code>patch</code>, <code>xz</code>, <code>libtoolize</code>, <code>which</code></td></tr><tr><td>RHEL / Fedora</td><td><code>perl</code>, <code>make</code>, <code>bison</code>, <code>re2c</code>, <code>flex</code>, <code>gperf</code>, <code>git</code>, <code>autoconf</code>, <code>automake</code>, <code>tar</code>, <code>unzip</code>, <code>gzip</code>, <code>gcc</code>, <code>g++</code>, <code>bzip2</code>, <code>cmake</code>, <code>patch</code>, <code>which</code>, <code>xz</code>, <code>libtool</code>, <code>gettext-devel</code>, <code>file</code></td></tr><tr><td>CentOS</td><td>The RHEL baseline plus the Perl <code>IPC::Cmd</code> and <code>Time::Piece</code> modules</td></tr><tr><td>Arch / Manjaro</td><td><code>base-devel</code>, <code>cmake</code>, <code>gperf</code></td></tr></tbody></table><p>Doctor additionally checks:</p><ul><li>CMake is at least version 3.22.</li><li><code>re2c</code> is at least version 1.0.3.</li><li><code>linux-headers</code> is available on a musl distribution.</li><li>StaticPHP&#39;s isolated <code>pkg-config</code> is installed and executable.</li></ul><p>The Linux default is <code>ZigToolchain</code>. When <code>--auto-fix</code> is enabled, Doctor can install the StaticPHP <code>zig</code> tool package when it is missing. When a musl target is built on a non-musl host, Doctor also checks the musl runtime/wrapper required by that target. The legacy <code>MuslToolchain</code> additionally expects the musl cross toolchain under <code>/usr/local/musl</code>. A native Alpine build does not need these cross-host components.</p><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>The target string and the host distribution are different concepts. A glibc distribution can produce a musl target through Zig, while a <code>*-gnu.*</code> target remains dynamically linked to the requested glibc ABI.</p></div><h2 id="macos" tabindex="-1">macOS <a class="header-anchor" href="#macos" aria-label="Permalink to “macOS”">​</a></h2><p>The default compiler toolchain is the system Clang supplied by Xcode or Xcode Command Line Tools. StaticPHP also requires either Homebrew or MacPorts to provide the remaining build tools.</p><p>Doctor currently checks these commands:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>curl, make, bison, re2c, flex, gperf, pkg-config, git,</span></span>
<span class="line"><span>autoconf, automake, tar, libtool, unzip, xz, gzip, bzip2,</span></span>
<span class="line"><span>cmake, glibtoolize</span></span></code></pre></div><p>It also requires GNU Bison 3 or later. If <code>/usr/bin/bison</code> is too old, Doctor searches the Homebrew and MacPorts locations and can install a newer package. On Apple Silicon, a Homebrew installation used by StaticPHP must be the native installation under <code>/opt/homebrew</code>, not an Intel installation under <code>/usr/local</code>.</p><p><code>SPC_USE_LLVM</code> controls the Clang variant:</p><table tabindex="0"><thead><tr><th>Value</th><th>Toolchain</th><th>Compiler location</th></tr></thead><tbody><tr><td><code>system</code> (default)</td><td><code>ClangNativeToolchain</code></td><td>Xcode / Command Line Tools <code>clang</code></td></tr><tr><td><code>brew</code></td><td><code>ClangBrewToolchain</code></td><td>Homebrew <code>opt/llvm/bin</code></td></tr><tr><td><code>port</code></td><td><code>ClangPortsToolchain</code></td><td>MacPorts <code>bin</code> (normally <code>/opt/local/bin</code>)</td></tr></tbody></table><p>When <code>brew</code> or <code>port</code> is selected, Doctor verifies that the corresponding LLVM installation contains <code>clang</code>.</p><h2 id="windows" tabindex="-1">Windows <a class="header-anchor" href="#windows" aria-label="Permalink to “Windows”">​</a></h2><p>Windows builds currently target x86-64 and use two kinds of prerequisites.</p><p>The following components must be installed by the user:</p><ul><li>Visual Studio with the x86/x64 C++ tools component.</li><li>Git for Windows, with <code>patch.exe</code> available on <code>PATH</code> (normally from <code>C:\\Program Files\\Git\\usr\\bin</code>).</li></ul><p>Doctor can install these StaticPHP-managed baseline packages:</p><table tabindex="0"><thead><tr><th>Package</th><th>Purpose</th></tr></thead><tbody><tr><td><code>vswhere</code></td><td>Locate the newest Visual Studio installation that contains <code>Microsoft.VisualStudio.Component.VC.Tools.x86.x64</code></td></tr><tr><td><code>msys2-build-essentials</code></td><td>Provide MSYS2 plus <code>make</code>, Autotools, <code>libtool</code>, <code>pkgconf</code>, Perl, Bison, and <code>re2c</code></td></tr><tr><td><code>7za-win</code></td><td>Extract formats that need <code>7za.exe</code></td></tr></tbody></table><p>Additional tools such as <code>nasm</code> and <code>strawberry-perl</code> are also represented as Tool packages, but they are installed when a selected package declares them through <code>tools</code>; they are not unconditional Windows Doctor checks.</p><p><code>MSVCToolchain</code> calls the detected Visual Studio <code>vcvarsall.bat x64</code> and imports its environment. Doctor then verifies <code>cl.exe</code>, <code>link.exe</code>, <code>lib.exe</code>, <code>dumpbin.exe</code>, <code>msbuild.exe</code>, and <code>nmake.exe</code>. The imported environment is cached in <code>downloads/.vcenv-cache</code> for up to one hour.</p><p>The MSYS2 root defaults to <code>pkgroot/.../msys2-build-essentials/msys64</code>. It can be changed with <code>SPC_MSYS2_PATH</code>; the value must point to the <code>msys64</code> directory. The old v2 PHP SDK directory and Visual Studio version options are not used in v3.</p><h3 id="llvm-clang-for-frankenphp" tabindex="-1">LLVM/Clang for FrankenPHP <a class="header-anchor" href="#llvm-clang-for-frankenphp" aria-label="Permalink to “LLVM/Clang for FrankenPHP”">​</a></h3><p>The normal Windows PHP and library build uses MSVC. FrankenPHP is the exception: its CGO link step requires Clang and LLD.</p><p>StaticPHP first accepts an absolute Clang path from <code>CC</code>. Otherwise it looks for the Visual Studio LLVM installation at <code>VC\\Tools\\Llvm\\x64\\bin\\clang.exe</code> using <code>vswhere</code>. Install the <strong>C++ Clang tools for Windows</strong> component in Visual Studio Installer when building FrankenPHP. This Clang requirement does not replace the MSVC requirement for the PHP and dependency-library stages.</p><h2 id="compiler-toolchain-selection" tabindex="-1">Compiler Toolchain Selection <a class="header-anchor" href="#compiler-toolchain-selection" aria-label="Permalink to “Compiler Toolchain Selection”">​</a></h2><p><code>GlobalEnvManager</code> loads <code>config/env.ini</code> (and an optional <code>config/env.custom.ini</code>) before <code>ToolchainManager</code> initializes the selected toolchain. The normal selection is:</p><table tabindex="0"><thead><tr><th>Host OS</th><th>Default</th><th>Other implementations currently present</th></tr></thead><tbody><tr><td>Linux</td><td><code>ZigToolchain</code></td><td><code>GccNativeToolchain</code>, <code>ClangNativeToolchain</code>, <code>MuslToolchain</code></td></tr><tr><td>macOS</td><td><code>ClangNativeToolchain</code></td><td><code>ClangBrewToolchain</code>, <code>ClangPortsToolchain</code></td></tr><tr><td>Windows</td><td><code>MSVCToolchain</code></td><td>None</td></tr></tbody></table><p>On Unix, the toolchain supplies defaults for <code>CC</code>, <code>CXX</code>, <code>AR</code>, <code>RANLIB</code>, and <code>LD</code>. Values already set by the caller take precedence over defaults from <code>env.ini</code>. <code>SPC_TARGET</code> determines the target OS/libc/ABI, while <code>SPC_USE_LLVM</code> chooses the macOS Clang distribution. <code>SPC_TOOLCHAIN</code> can override the implementation class, but it is currently a maintainer-level/internal setting rather than a stable public extension contract.</p><p>After environment initialization, the toolchain validates its commands and target/libc combination. It also records a compiler description in <code>PHP_BUILD_COMPILER</code> when one can be determined.</p><h2 id="isolated-pkg-config" tabindex="-1">Isolated pkg-config <a class="header-anchor" href="#isolated-pkg-config" aria-label="Permalink to “Isolated pkg-config”">​</a></h2><p>Unix builds deliberately use a StaticPHP-managed <code>pkg-config</code>, rather than whichever executable happens to be first on the system <code>PATH</code>. <code>PkgConfigUtil</code> searches, in order:</p><ol><li><code>pkgroot/&lt;platform&gt;/bin/pkg-config</code></li><li><code>buildroot/bin/pkg-config</code></li></ol><p>Doctor can install a pre-built copy through the <code>pkg-config</code> tool package. If a source build is needed, StaticPHP builds pkg-config with its internal GLib and disables the system include, library, sysroot, and default <code>.pc</code> search paths.</p><p>During a build, <code>PKG_CONFIG_PATH</code> starts with <code>buildroot/lib/pkgconfig</code>, and StaticPHP&#39;s dependency queries and generated CMake toolchain file request <code>--static</code> flags. This prevents an unrelated system library from silently satisfying a dependency that StaticPHP intended to build itself.</p><h2 id="package-build-executors" tabindex="-1">Package Build Executors <a class="header-anchor" href="#package-build-executors" aria-label="Permalink to “Package Build Executors”">​</a></h2><p>Package classes use three common executor implementations:</p><table tabindex="0"><thead><tr><th>Executor</th><th>Platform and build system</th><th>Current behavior</th></tr></thead><tbody><tr><td><code>UnixAutoconfExecutor</code></td><td>Linux/macOS, Autoconf</td><td>Adds static/PIC and <code>buildroot</code> prefix arguments, runs <code>configure</code>, parallel <code>make</code>, and optionally <code>make install</code></td></tr><tr><td><code>UnixCMakeExecutor</code></td><td>Linux/macOS, CMake</td><td>Generates a toolchain file rooted at <code>buildroot</code>, pins StaticPHP&#39;s compilers and pkg-config, disables shared libraries, then configures, builds, and installs</td></tr><tr><td><code>WindowsCMakeExecutor</code></td><td>Windows, CMake/MSVC</td><td>Uses the x64 Visual Studio generator, the static MSVC runtime, a StaticPHP CMake toolchain file, and <code>cmake --build --target install</code></td></tr></tbody></table><p>The executors initialize package-specific include, library, and environment paths and use the configured build concurrency. Unix Autoconf failures preserve <code>config.log</code>, and the Unix CMake executor copies available configure/error/output logs to <code>SPC_LOGS_DIR</code>. The Windows CMake executor currently relies on the general shell and output logs instead of collecting those CMake files separately.</p><p>These classes are useful landmarks when maintaining built-in packages, but their constructors and fluent methods are not yet declared a stable PHP API. Package authors should follow existing <code>src/Package/Library</code> or <code>src/Package/Tool</code> implementations until the public extension API is defined.</p><h2 id="diagnosing-tool-failures" tabindex="-1">Diagnosing Tool Failures <a class="header-anchor" href="#diagnosing-tool-failures" aria-label="Permalink to “Diagnosing Tool Failures”">​</a></h2><p>When Doctor passes but a package still fails:</p><ol><li>Run <code>spc dev:shell</code> (or <code>bin/spc dev:shell</code>) and check <code>CC</code>, <code>CXX</code>, <code>AR</code>, <code>LD</code>, <code>PKG_CONFIG</code>, and <code>PATH</code>.</li><li>Confirm that <code>SPC_TARGET</code> describes the intended libc and ABI.</li><li>Read <code>log/spc.output.log</code>, <code>log/spc.shell.log</code>, and any package-specific Autoconf/CMake logs.</li><li>On Windows, re-run Doctor if Visual Studio, MSYS2, or LLVM components changed; remove the one-hour <code>downloads/.vcenv-cache</code> only when a stale Visual Studio environment is suspected.</li></ol><p>Do not work around a missing tool by copying binaries into <code>buildroot/</code> or editing generated build directories. Add it as a host prerequisite, a StaticPHP tool package, or package build dependency according to its role.</p>`,60)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};