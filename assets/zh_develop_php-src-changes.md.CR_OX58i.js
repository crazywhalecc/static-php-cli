import{G as e,K as t,at as n,j as r}from"./chunks/framework.DrNIf5Lg.js";var i=JSON.parse(`{"title":"对 PHP 源码的修改","description":"","frontmatter":{},"headers":[],"relativePath":"zh/develop/php-src-changes.md","filePath":"zh/develop/php-src-changes.md"}`),a={name:`zh/develop/php-src-changes.md`};function o(r,i,a,o,s,c){return n(),e(`div`,null,[...i[0]||=[t(`<h1 id="对-php-源码的修改" tabindex="-1">对 PHP 源码的修改 <a class="header-anchor" href="#对-php-源码的修改" aria-label="Permalink to “对 PHP 源码的修改”">​</a></h1><p>StaticPHP 会修改解压后的 PHP 源码树，以支持静态链接、额外的 SAPI、较旧的 PHP 分支以及特定平台工具链。这些改动应用于 <code>source/php-src</code> 下生成的工作区；<code>downloads/</code> 中下载的归档文件不会被修改。</p><p>本页描述 <code>core</code> registry 自带的补丁。它是供维护者使用的实现清单，并不承诺 <code>SourcePatcher</code>、生命周期回调签名或单个补丁名称属于稳定的公开 API。</p><h2 id="修改发生的时机" tabindex="-1">修改发生的时机 <a class="header-anchor" href="#修改发生的时机" aria-label="Permalink to “修改发生的时机”">​</a></h2><p>修改会在以下几个时机进入源码树：</p><ol><li><strong>解压 <code>php-src</code> 后</strong>：Artifact hook 会在构建任何 Package 前应用兼容性修改。</li><li><strong><code>buildconf</code> / configure 前</strong>：PHP target 会应用版本化补丁集并改写构建系统输入。</li><li><strong>构建某个 SAPI 或扩展前</strong>：条件 hook 会修改生成的 Makefile 或扩展源码。</li><li><strong>围绕某个构建阶段临时应用</strong>：少量改动会先备份，并在该 SAPI 构建结束后恢复。</li></ol><p>带有 <code>#[PatchDescription]</code> 的方法由 StaticPHP 通过 lifecycle/DI dispatcher 调用时，会输出 <code>[PATCH]</code> 消息，便于追踪构建过程。但它不是完整的机器可读清单：直接方法调用不会经过该日志逻辑，仍有少量源码改写和生成文件修改也没有该标注。</p><h2 id="解压后应用的修改" tabindex="-1">解压后应用的修改 <a class="header-anchor" href="#解压后应用的修改" aria-label="Permalink to “解压后应用的修改”">​</a></h2><p>每次解压全新的 <code>php-src</code> Artifact 时，都会运行 <code>src/Package/Artifact/php_src.php</code>。</p><table tabindex="0"><thead><tr><th>源码版本或条件</th><th>修改</th></tr></thead><tbody><tr><td>PHP 低于 8.0</td><td>应用旧版 Alpine/musl 构建使用的 stream cast 兼容补丁</td></tr><tr><td>PHP 8.0.x</td><td>对 DOM、libxml 和 SOAP 应用 libxml2 2.12 兼容补丁，并应用旧版 stream cast 补丁</td></tr><tr><td>PHP 8.1.x</td><td>应用旧版 stream cast 补丁</td></tr><tr><td>所有受支持 PHP 分支中的 GD</td><td>使用 StaticPHP 针对不同版本的静态构建配置替换 <code>ext/gd/config.w32</code>；低于 PHP 8.2 时还会对 <code>gdft.c</code> 应用 <code>_WIN32</code> 兼容修复</td></tr><tr><td>内置 IMAP 源码缺少许可证文件</td><td>添加内置 Apache 许可证文本作为 <code>ext/imap/LICENSE</code>，供许可证收集使用</td></tr></tbody></table><p>GD 和 IMAP 操作修改的是只有在相应扩展存在或参与构建时才有作用的文件。解压 hook 本身会在解压 PHP 源码时运行，此时扩展解析尚未进入构建 stage。</p><h2 id="版本化-php-补丁集" tabindex="-1">版本化 PHP 补丁集 <a class="header-anchor" href="#版本化-php-补丁集" aria-label="Permalink to “版本化 PHP 补丁集”">​</a></h2><p>在 <code>buildconf</code> 前，Unix 和 Windows 构建都会调用 <code>SourcePatcher::patchPhpSrc()</code>。尽管它沿用了“micro patches”这一历史名称，但实际会用于整个 PHP 构建，并不要求选择 <code>php-micro</code>。</p><p><code>config/env.ini</code> 中默认的 <code>SPC_MICRO_PATCHES</code> 值如下：</p><table tabindex="0"><thead><tr><th>宿主平台</th><th>默认补丁名称</th></tr></thead><tbody><tr><td>Linux</td><td><code>cli_checks</code>、<code>disable_huge_page</code></td></tr><tr><td>macOS</td><td><code>cli_checks</code>、<code>macos_iconv</code></td></tr><tr><td>Windows</td><td><code>static_extensions_win32</code>、<code>cli_checks</code>、<code>disable_huge_page</code>、<code>vcruntime140</code>、<code>win32</code>、<code>zend_stream</code>、<code>cli_static</code>、<code>win32_api</code></td></tr></tbody></table><p>这些补丁当前的用途如下：</p><table tabindex="0"><thead><tr><th>补丁</th><th>用途</th></tr></thead><tbody><tr><td><code>cli_checks</code></td><td>在原本直接将 SAPI 名称与 <code>cli</code> 比较的 PHP 代码路径中，把 micro SAPI 视作 CLI</td></tr><tr><td><code>disable_huge_page</code></td><td>禁用 Linux 的 2 MiB segment alignment 探测，避免 SFX 二进制无谓增大</td></tr><tr><td><code>macos_iconv</code></td><td>让 PHP 的 iconv 检查正确链接 macOS iconv 库</td></tr><tr><td><code>static_extensions_win32</code></td><td>调整包括 Fileinfo 和 OpenSSL 在内的 Windows 扩展配置，以支持静态构建</td></tr><tr><td><code>cli_static</code></td><td>调整 Windows CLI 源码，以生成完全静态的可执行文件</td></tr><tr><td><code>vcruntime140</code></td><td>绕过启动时对动态加载 <code>vcruntime140</code> 模块的检查</td></tr><tr><td><code>win32</code></td><td>把 PHP Windows 构建配置从动态 MSVC runtime（<code>/MD</code>）切换到静态 runtime（<code>/MT</code>）</td></tr><tr><td><code>zend_stream</code></td><td>避免 <code>isatty</code> 声明与 Windows 静态 CRT 冲突</td></tr><tr><td><code>win32_api</code></td><td>修正受支持 PHP 分支所需的 Windows API 声明</td></tr></tbody></table><p>补丁文件位于 <code>src/globals/patch/php-src-patches/</code>。同一名称可以只有不带版本的文件，如 <code>zend_stream.patch</code>；也可以包含 <code>cli_checks_84.patch</code> 这样的版本文件。对于带版本的名称，StaticPHP 会选择不高于目标源码 minor 版本的最新补丁。例如，可用 <code>_81</code> 和 <code>_84</code> 但没有 <code>_82</code> 时，PHP 8.2 源码会使用 <code>_81</code>。PHP 7.4 会跳过该补丁集加载器。</p><p>该目录还包含 <code>comctl32</code>、<code>phar</code> 和 <code>static_opcache</code> 等非默认或按条件选择的补丁。找不到兼容补丁或执行 <code>patch -p1</code> 失败时，构建会以 <code>PatchException</code> 终止。</p><h2 id="通用构建期修改" tabindex="-1">通用构建期修改 <a class="header-anchor" href="#通用构建期修改" aria-label="Permalink to “通用构建期修改”">​</a></h2><p>除下方各平台表格中的改动外，PHP target 还会应用两个通用修改。</p><h3 id="staticphp-版本元数据" tabindex="-1">StaticPHP 版本元数据 <a class="header-anchor" href="#staticphp-版本元数据" aria-label="Permalink to “StaticPHP 版本元数据”">​</a></h3><p>在 PHP <code>build</code> stage 前，如果源码尚未包含相关内容，StaticPHP 会在 <code>main/main.c</code> 中添加名为 <code>StaticPHP.version</code> 的 INI 条目。这样可以在运行时获取生成该二进制的 StaticPHP 版本。</p><h3 id="硬编码-ini-值" tabindex="-1">硬编码 INI 值 <a class="header-anchor" href="#硬编码-ini-值" aria-label="Permalink to “硬编码 INI 值”">​</a></h3><p>每个 <code>--with-hardcoded-ini=key=value</code> 选项都会向所有实际存在且受支持的 SAPI 源码中的 <code>HARDCODED_INI</code> 添加一行字面值：</p><ul><li><code>sapi/cli/php_cli.c</code></li><li><code>sapi/micro/php_micro.c</code></li><li><code>sapi/embed/php_embed.c</code></li></ul><p>原文件会保存为带 <code>.bak</code> 后缀的备份。后续调用再次提供非空值集合时，StaticPHP 会先恢复备份，再应用新的集合，因此从一组配置值改成另一组时不会累积旧值。当前完全不再传入任何 <code>--with-hardcoded-ini</code> 选项时不会触发恢复逻辑；要移除先前注入的值，需要重新解压 <code>php-src</code>。</p><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>硬编码值会成为源码和最终二进制的一部分。不要使用该选项保存密码、token 或其他秘密信息。</p></div><h2 id="unix-构建期修改" tabindex="-1">Unix 构建期修改 <a class="header-anchor" href="#unix-构建期修改" aria-label="Permalink to “Unix 构建期修改”">​</a></h2><p>Linux 和 macOS 构建会应用以下与目标相关的修改：</p><table tabindex="0"><thead><tr><th>条件</th><th>文件或生成的输入</th><th>修改</th></tr></thead><tbody><tr><td>每次 Unix PHP 构建</td><td><code>configure.ac</code></td><td>使用 <code>SPC_TARGET</code> 选择的 libc 取代基于宿主机 <code>ldd</code> 的 musl 检测；原文件会被备份</td></tr><tr><td>每次 Unix PHP 构建</td><td><code>build/php.m4</code> 和扩展的 <code>config.m4</code> 文件</td><td>把 <code>PKG_CHECK_MODULES</code> 替换为 <code>PKG_CHECK_MODULES_STATIC</code>，使依赖检查包含静态链接所需的 private 依赖</td></tr><tr><td>PHP 8.3.x</td><td><code>build/php.m4</code></td><td>应用 PHP 8.4 以前使用的 AVX-512 configure cache 兼容补丁</td></tr><tr><td>PHP 低于 8.3</td><td>生成的 <code>configure</code></td><td>使用受支持的 <code>-std=gnu17</code> 探测，而不是较新的 <code>-std=gnu23</code> 探测</td></tr><tr><td>动态链接的 musl 目标</td><td><code>TSRM/TSRM.h</code></td><td>为主 TSRM TLS cache 符号添加 default visibility，使采用 <code>initial-exec</code> TLS model 的共享扩展可以加载</td></tr><tr><td>Linux</td><td>生成的 <code>Makefile</code></td><td>把意外出现的 <code>//lib</code> 路径规范化为 <code>/lib</code></td></tr><tr><td>使用 Zig 的 Linux</td><td>生成的 <code>Makefile</code></td><td>让 <code>BUILD_CC</code> 使用宿主机 <code>cc</code> 而不是 <code>zig-cc</code>，避免 minilua 等宿主工具被构建为目标环境程序</td></tr><tr><td>存在 release 链接参数</td><td><code>ext/standard/info.c</code></td><td>从 PHP 信息输出中隐藏 configure 命令</td></tr></tbody></table><p>解析到 <code>php-micro</code> 时，其 Package hook 还会调整生成的 Makefile：embed 前置构建只生成 <code>libphp.la</code>，并防止 PHP 主安装步骤过早安装 micro 二进制。</p><p>如果 Unix micro 构建选择了 <code>ext-phar</code>，<code>ext/phar/phar.c</code> 会被临时修改，使追加到当前可执行文件的压缩 PHAR 即使文件名不含 <code>.phar</code> 也能被识别。micro stage 结束后会立即恢复原文件。</p><h2 id="windows-构建期修改" tabindex="-1">Windows 构建期修改 <a class="header-anchor" href="#windows-构建期修改" aria-label="Permalink to “Windows 构建期修改”">​</a></h2><p>Windows 需要更大范围的构建系统修改，因为 PHP 上游的 Windows 流程通常假定使用动态 CRT、PHP SDK 工具以及基于 import library 的 SAPI。</p><table tabindex="0"><thead><tr><th>Stage 或条件</th><th>修改</th></tr></thead><tbody><tr><td><code>buildconf.bat</code> 前</td><td>从相应的 Windows core 构建规则中移除共享 <code>dllmain.c</code> 对象，然后应用版本化补丁集</td></tr><tr><td>缺少 <code>win32/wsyslog.h</code></td><td>使用 <code>win32/build/wsyslog.mc</code> 定义的 event ID 生成兼容头文件</td></tr><tr><td>PHP 8.1.x</td><td>把 Fiber 汇编对象从链接器参数移动到汇编对象列表，使其参与静态链接</td></tr><tr><td>检测到 Visual Studio</td><td>改写 <code>win32/build/confutils.js</code>，使受支持的 Visual Studio 安装报告正确的 PHP toolset 名称</td></tr><tr><td><code>buildconf.bat</code> 后</td><td>禁用生成的 <code>configure.js</code> 中的 PHP SDK 版本检查，因为 v3 使用 MSVC 加自管 MSYS2 环境，而不是 PHP SDK binary tools</td></tr><tr><td><code>--enable-micro-win32</code></td><td>在 <code>sapi/micro/php_micro.c</code> 中定义 <code>PHP_MICRO_WIN32_NO_CONSOLE</code>；选项不再启用时会恢复备份</td></tr><tr><td>CLI 构建</td><td>改写生成的 <code>php.exe</code> 规则，直接链接 PHP core、CLI SAPI、静态扩展、汇编对象和静态库；同时把 <code>buildroot/include</code> 参数放到扩展参数之前</td></tr><tr><td>CGI 构建</td><td>改写生成的 <code>php-cgi.exe</code> 规则以支持静态链接，并从 CGI 源码中移除重复的 <code>ZEND_TSRMLS_CACHE_DEFINE()</code></td></tr><tr><td>Micro 构建</td><td>添加生成的 micro target 所需的 Fiber 汇编前置依赖，并提供静态库集合</td></tr><tr><td>Embed 构建</td><td>改写生成的 embed target，使 <code>phpNembed.lib</code>（例如 <code>php8embed.lib</code>）包含 embed SAPI、PHP core、静态扩展和汇编对象，而不再只是 import library</td></tr></tbody></table><p>最后一项 embed 修改也是 Windows FrankenPHP 得以构建的基础：FrankenPHP 通过 CGO 使用 Clang/LLD 链接这个自包含的 embed 库。Unix FrankenPHP 不会应用单独的 php-src 补丁；它直接使用常规 PHP target 生成的 Unix <code>php-embed</code> 库。</p><h2 id="条件式扩展补丁" tabindex="-1">条件式扩展补丁 <a class="header-anchor" href="#条件式扩展补丁" aria-label="Permalink to “条件式扩展补丁”">​</a></h2><p>扩展被解析到构建集合后，其 Package 类可以修改 <code>php-src/ext/&lt;name&gt;</code> 下的源码。两个跨版本的典型例子是：</p><ul><li><strong>Opcache</strong>：<code>ext-opcache</code> 会为 PHP 8.0–8.4.x 应用相应版本的静态 Opcache 补丁。PHP 8.2.23 以前和 PHP 8.3.11 以前使用专用 legacy 补丁，其余版本使用版本化的 <code>static_opcache</code> 补丁。PHP 8.5 及以后无需该补丁即可使用所需的静态路径。</li><li><strong>Micro 的 PHAR</strong>：只有 <code>ext-phar</code> 参与构建时才会应用前文所述的 Unix micro hook，并在生成 micro 二进制后恢复原文件。</li></ul><p>其他扩展类还包含针对特定 PHP、编译器、依赖库或操作系统版本的更小范围兼容修改。这些修改会刻意保留在 <code>src/Package/Extension/</code> 中相应扩展的构建逻辑旁，而不加入全局 PHP 补丁集。可以搜索 <code>#[PatchDescription]</code> 查看当前带标注的清单。</p><h2 id="补丁应用与恢复" tabindex="-1">补丁应用与恢复 <a class="header-anchor" href="#补丁应用与恢复" aria-label="Permalink to “补丁应用与恢复”">​</a></h2><p><code>SourcePatcher::patchFile()</code> 会把相对补丁名称解析到 <code>src/globals/patch/</code>，通过 reverse dry run 检查补丁是否已经应用，然后调用 <code>patch --binary -p1</code>。因此，在同一解压源码树中重复应用基于文件的补丁是安全的。直接字符串替换也按可重复调用的方式编写，再次执行时通常已经找不到需要替换的原始文本。</p><p>构建后不存在恢复整个 PHP 源码树的事务。只有 PHAR 和硬编码 INI 备份等明确的临时改动带有专用恢复逻辑。要回到干净的上游源码，请删除并重新解压 <code>source/php-src</code>；切换或刷新 PHP 版本时，也可以使用不带 <code>--keep-source</code> 的 <code>switch-php-version</code>。</p><p>v2 的任意 patch point 注入机制不是受支持的 v3 接口。在公开补丁扩展契约确定前，新增 core 补丁应实现为由维护者维护的 Artifact 或 Package hook。</p><h2 id="添加或更新-core-补丁" tabindex="-1">添加或更新 Core 补丁 <a class="header-anchor" href="#添加或更新-core-补丁" aria-label="Permalink to “添加或更新 Core 补丁”">​</a></h2><p>维护内置补丁时：</p><ol><li>优先推动上游修复；只有仍受支持的 PHP 分支确实需要时才保留下游补丁。</li><li>可复用补丁文件放在 <code>src/globals/patch/</code>；需要按版本选择的 micro/static-PHP 补丁集放在 <code>src/globals/patch/php-src-patches/</code>。</li><li>使用精确的 PHP/平台/依赖条件，避免无关构建无谓修改源码。</li><li>确保操作可重复执行，并在预期的上游上下文已经变化时明确失败。</li><li>对通过 lifecycle/DI dispatcher 调用的方法添加 <code>#[PatchDescription]</code>，使该修改在构建日志中可见；直接调用的方法应显式记录日志。</li><li>测试该补丁影响的最旧和最新 PHP 分支；行为变化时同步更新本页的两个语言版本。</li></ol>`,48)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};